specVersion: "v1"
serviceId: "gradready"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "gradready-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
